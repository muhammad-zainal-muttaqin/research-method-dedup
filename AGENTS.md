# AGENTS.md

Agent guidance for this repo lives in [CLAUDE.md](./CLAUDE.md).

This file is intentionally a thin pointer so agent frameworks that look for `AGENTS.md` resolve to the single source of truth in `CLAUDE.md` and don't drift out of sync. Earlier full copy (mirror of `CLAUDE.md` as of 2026-05-10) is recoverable from git history (`git show HEAD:AGENTS.md`).
