dataset/
__pycache__/
*.pyc
.DS_Store
__MACOSX/
Thumbs.db
desktop.ini

# tools_sawit dev sandbox
tools_sawit/_test_fixtures/
tools_sawit/_test_output/
tools_sawit/debug.log

# large raw export folders (too many files for git)
05 Mei 2026/
archive/

# new YOLO dataset: track labels/json/yaml only, not images
Brand-New-Dataset-YOLO/images/

# intermediate backup folders (zipped to archive.zip)
*.backup_*/

# consolidated zip snapshot at repo root
archive.zip

# scratch / temp scripts at repo root
/_tmp_*.py
/_chk*.py
/_dedup_*.py
/_merge*.py
/_search_*.py
/_verify_*.py

# training detection dataset (mirror of HF ULM-DS-Lab/OilPalm-MultiView-BunchCount-YOLO)
# clone fresh dari HF di RunPod, jangan commit
Tested-Brand-New-Dataset-YOLO/

# zip backups (heavy)
*.zip

# Ultralytics training output (weights, plots, results.csv per run)
runs/

# YOLO weight files (download/train fresh, jangan commit)
*.pt
*.engine
*.onnx

# HF token + secrets
.env
*.token
