# OS / editor cruft
.DS_Store
__MACOSX/
Thumbs.db
desktop.ini

# Python build artefak
__pycache__/
*.pyc

# Historical / archive (never commit)
archive/

# Brand-New-Dataset-YOLO: track labels/json/yaml only, not images (heavy, distributed via HF)
Brand-New-Dataset-YOLO/images/

# Backup folders (zipped to archive)
*.backup_*/

# Scratch / temp scripts at repo root
/_tmp_*.py
/_chk*.py
/_dedup_*.py
/_merge*.py
/_search_*.py
/_verify_*.py

# Zip backups (heavy) + Ultralytics training output
*.zip
runs/

# YOLO weight files — commit ml-track/baseline-run/weights/*.pt explicitly
*.pt
!ml-track/baseline-run/weights/*.pt
*.engine
*.onnx

# Secrets
.env
*.token
